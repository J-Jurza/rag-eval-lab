# Copy to .env and fill in. Only the generation and judge steps need this;
# retrieval and retrieval evaluation run with no key at all.
GEMINI_API_KEY=
# Override the model if you want; the default is set in src/rag_eval_lab/generate.py
# GEMINI_MODEL=gemini-2.5-flash
